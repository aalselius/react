import React from 'react';
import Nav from './Nav.js';

const Sidebar = (props) => {
    return (
      <div className="sidebar">
        <Nav />
      </div>
    );
}

export default Sidebar;